apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gomodel.fullname" . }}-test-connection"
  labels:
    {{- include "gomodel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gomodel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
