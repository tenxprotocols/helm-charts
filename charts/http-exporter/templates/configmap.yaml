{{- if not .Values.existingConfigMap -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "http-exporter.fullname" . }}
  labels:
    {{- include "http-exporter.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Values.config | toYaml | nindent 4 }}
{{- end }}
