apiVersion: v1
kind: Service
metadata:
  name: {{ include "http-exporter.fullname" . }}
  labels:
    {{- include "http-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "http-exporter.selectorLabels" . | nindent 4 }}
