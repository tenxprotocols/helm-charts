apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "http-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "http-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "http-exporter.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
