{{- if .Values.marketing.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.namePrefix }}-marketing
  labels:
    {{- include "swarmforge.componentLabels" (dict "component" "marketing" "context" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.marketing.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swarmforge.componentSelectorLabels" (dict "component" "marketing" "context" .) | nindent 4 }}
{{- end }}
