---
apiVersion: v2
name: tezos-baker-tail
description: Live per-level baker classifier that subscribes to a Tezos node's
  head stream and emits JSON events plus Prometheus metrics.

maintainers:
  - name: tenxprotocols
    email: software@tenx.inc
    url: https://github.com/tenxprotocols

type: application
version: 1.0.0
appVersion: "0.1.0"
